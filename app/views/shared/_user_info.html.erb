</header>
<a href="<%= user_path(current_user) %>">
  <%= image_tag(current_user.image_url, alt: current_user.name) %>
</a>
<h1><%= current_user.name %></h1>
<span><%= link_to "view my profile", current_user %></span>
<span><%= pluralize(current_user.goals.count,"goal")%></span>
<span><%= pluralize(current_user.goals.count,"post")%></span>
<span><%= pluralize(current_user.goals.count,"motivation")%> </span>
